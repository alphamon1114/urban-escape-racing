# 검증 기록

검증 환경: Windows, Unity 6000.6.0f1. 2026-09-13.

아래 초기 기록은 drift 설정 당시 결과다. 최신 차량 설정과 검증은 문서 끝의 **고속 언더스티어 변경 (2026-09-14)** 항목을 따른다.

## 자동 검사

Unity Editor를 `-batchmode -nographics -quit -executeMethod PrototypeSetup.CreateScene`으로 실행했습니다. 그래픽 없는 Editor 물리 검사이며 Play 모드의 키 입력을 재현하는 검사는 아닙니다. 실제 씬을 로드한 후 차량 초기화 및 FixedUpdate를 호출하고 Physics.Simulate로 물리를 진행합니다.

| 항목 | 결과 |
|---|---|
| 스크립트 컴파일 및 씬 생성 | 통과 |
| 차량 지면 안착 | 전륜·후륜 접촉 확인 |
| 12초 가속 | 약 140.3 km/h |
| 이어서 4초 제동 | 약 50.7 km/h로 감소 |
| S 입력 유지에 해당하는 제동 상태 | 후진 약 -8.6 m/s 확인 |
| 첫 조향 스텝 | 1.70도, 즉시 최대 조향하지 않음 |
| 저속 / 고속 조향 | 32.0도 / 32.0도 (속도 제한 제거) |
| 핸드브레이크 | 후륜 제동 4,200 Nm 및 후륜 구동력 차단 확인 |
| 복귀 | 선속도·각속도 초기화 확인 |

상세 실행 로그는 로컬 `oversteer-verify.log`에 남고 Git에서는 제외됩니다. 측정은 기본 약 0.02초 물리 스텝을 사용합니다. 검사 과정에서 변한 씬 상태는 저장하지 않습니다.

## 미검증 항목

- 화면 렌더링, Game 뷰 키보드 포커스 및 실제 키 이벤트.
- 실제 플레이에서의 무게감·속도감, 고속 오버스티어 강도와 감속 후 회복감.
- 카메라 움직임의 편안함, 장시간 주행 안정성.
- Windows 독립 실행 파일 빌드.

타이어 설정은 오버스티어와 감속 후 접지 회복을 의도하지만 위 자동 검사만으로 주관적 주행감을 보장하지 않습니다. README의 코너링 비교 절차로 평가 후 튜닝하는 단계입니다.

## 오버스티어 변경 검증

열려 있는 Unity 프로젝트를 보존하기 위해 .physics-check 복사본에서 동일 Unity 버전으로 컴파일 및 물리 검사를 재실행했습니다. 저속과 고속의 최대 조향각 모두 32도를 확인했습니다. 속도 기반 조향 제한은 사용하지 않습니다. 이후 카운터스티어 변경에서 조건부 회전 감쇠 보조를 추가했습니다. 실제 드리프트 유지와 감속·카운터스티어 회복감은 직접 플레이 튜닝 대상입니다.


## 카운터스티어 완화 검증

Unity 6000.6.0f1 복사본 컴파일 및 물리 검사 통과 (countersteer-verify.log, 종료 코드 0). 고속 코너 후륜 최대 slip은 동일 검사에서 0.510 → 0.278로 감소했습니다. 전진 20m/s·횡속도 -8m/s·초기 회전 1.2rad/s 상태에서 반대키를 0.2초 적용한 비교는 보조 OFF 1.358rad/s, ON 0.764rad/s로 회전 감소를 확인했습니다. 이는 인위적으로 설정한 한 가지 미끄러짐의 비교이며 모든 코너에서의 회복을 보장하지 않습니다. 기존 가속·제동·후진·조향·핸드브레이크·복귀 검사도 통과했습니다. 실제 키보드 주행감은 플레이 확인이 필요합니다.


## 핸드브레이크 조향 변경

Unity 복사본에서 컴파일·기존 물리 검사 및 핸드브레이크 검사를 통과했습니다 (handbrake-verify.log, 종료 코드 0). 20m/s에서 W+Space+우측 조향을 0.5초 적용한 회전은 1.448rad/s, 이후 반대키 0.5초 적용 시 0.626rad/s로 감소했습니다. 전륜 구동 유지·후륜 구동 차단, Space 해제 즉시 브레이크 해제, 접지 강도 0.671에서 1.12까지 점진 복귀를 확인했습니다. 실제 화면에서의 드리프트 느낌과 지속 드리프트는 직접 플레이로 확인해야 합니다.

## 고속 언더스티어 변경 (2026-09-14)

사용자 피드백에 따라 고속 일반 조향에서는 앞이 밀리고, 핸드브레이크로 뒤를 흘린 뒤 반대키로 회복하는 방향으로 변경했다. 저속/고속 조향 한도는 32/7도(30~140km/h 구간 보간), 일반 조향 속도는 85~29.75도/초다. 실제 슬라이드를 회복하는 반대 조향에서는 최대 32도까지 조향 여유를 돌려준다. 평상시 전륜/후륜 횡접지 강도는 1.05/1.35이며 Space 중 후륜은 0.65까지 내려간다. 가속·제동 토크, 복귀 위치, 씬과 카메라는 변경하지 않았다.

열린 Unity 작업을 보존하기 위해 `.physics-check/understeer` 복사본에서 Unity 6000.6.0f1로 실행했다. `HandlingValidation.Run`과 `CityOutskirtsValidation.Run` 모두 컴파일 및 검사 통과, 종료 코드 0.

### 변경 전후 비교

DrivingTest의 저장된 차량을 매번 새로 로드하고 지면에 안착시킨 뒤 초기 전진 속도를 설정한다. 장애물 콜라이더는 검사 메모리에서만 끄며, W와 조향 입력에 해당하는 필드를 설정해 0.02초 간격으로 2초 시뮬레이션한다. 좌우 양방향에서 아래 반올림 수치가 같았다. 이 비교는 키보드 입력이나 실제 코스 완주가 아니다.

| 조건 및 측정값 | 기존 drift | 변경 후 |
|---|---:|---:|
| 144km/h 일반 조향: 최대 차체 슬립각 | 88.4° | 15.2° |
| 144km/h 일반 조향: 최대 회전 속도 | 1.446rad/s | 0.866rad/s |
| 144km/h 일반 조향: 2초 후 속도 | 93.4km/h | 134.9km/h |
| 72km/h 일반 조향: 최대 차체 슬립각 | 51.9° | 10.3° |

144km/h 일반 조향에서 전륜/후륜 최대 sidewaysSlip은 0.370/0.277로 앞 타이어가 먼저 한계에 도달하는 성향을 확인했다. 고속에서 1초 후 반대 방향으로 전환한 경우 최대 차체 슬립각 18.1°, 최대 회전 1.089rad/s로 검사 범위 안에서 스핀 없이 통과했다.

72km/h에서 Space를 0.5초 누른 뒤 해제하면 후륜 최대 slip은 일반 조향 0.226에서 0.582로 커졌다. 동시에 반대키를 적용한 비교에서는 2초 후 최초 진입 방향으로 돌아간 각도가 65.7°에서 30.8°로 줄고 최종 회전은 -0.089rad/s였다. 144km/h에서도 핸드브레이크 진입과 반대 조향 회복 검사를 통과했다. 반대키를 계속 유지하면 반대쪽으로 회전할 수 있으므로 실제 운전에서는 자세가 잡힐 때 입력을 풀어야 한다.

### CityOutskirts 회귀 검사

씬 객체 1,908개 및 차량·휠·카메라·HUD 참조 검사 통과. 가속 140.3km/h, 제동 후 50.7km/h, 후진 -8.6m/s, 첫 조향 1.70°, 저속/고속 조향 32/7°. 지면 안착, 핸드브레이크 전륜 구동 유지·후륜 차단, 해제 후 후륜 접지 1.35 복귀, R 복귀 검사를 통과했다. 카운터스티어 보조 OFF/ON 회전은 0.983/0.501rad/s였다.

재실행 메뉴: `Urban Escape > Validate Handling`, `Urban Escape > Validate City Outskirts`. 배치 실행 메서드는 각각 `HandlingValidation.Run`, `CityOutskirtsValidation.Run`이다. 로그는 `Logs/handling-understeer.log`, `Logs/city-understeer.log`, 이전 수치 발췌는 `Logs/handling-before-summary.txt`에 보관한다(Git 제외).

새 설정의 실제 키보드 주행감, 코스 완주, 렌더링과 FPS는 이번 자동 검사의 범위 밖이며 Play에서 확인해야 한다. 포르자의 차량 물리를 재현했다는 의미는 아니다.


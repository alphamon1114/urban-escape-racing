# 검증 기록

검증 환경: Windows, Unity 6000.6.0f1. 2026-09-13.

## 자동 검사

Unity Editor를 `-batchmode -nographics -quit -executeMethod PrototypeSetup.CreateScene`으로 실행했습니다. 그래픽 없는 Editor 물리 검사이며 Play 모드의 키 입력을 재현하는 검사는 아닙니다. 실제 씬을 로드한 후 차량 초기화 및 FixedUpdate를 호출하고 Physics.Simulate로 물리를 진행합니다.

| 항목 | 결과 |
|---|---|
| 스크립트 컴파일 및 씬 생성 | 통과 |
| 차량 지면 안착 | 전륜·후륜 접촉 확인 |
| 12초 가속 | 약 140.3 km/h |
| 이어서 4초 제동 | 약 50.7 km/h로 감소 |
| S 입력 유지에 해당하는 제동 상태 | 후진 약 -8.6 m/s 확인 |
| 첫 조향 스텝 | 1.70도, 즉시 최대 조향하지 않음 |
| 저속 / 고속 조향 | 32.0도 / 32.0도 (속도 제한 제거) |
| 핸드브레이크 | 후륜 제동 4,200 Nm 및 후륜 구동력 차단 확인 |
| 복귀 | 선속도·각속도 초기화 확인 |

상세 실행 로그는 로컬 `oversteer-verify.log`에 남고 Git에서는 제외됩니다. 측정은 기본 약 0.02초 물리 스텝을 사용합니다. 검사 과정에서 변한 씬 상태는 저장하지 않습니다.

## 미검증 항목

- 화면 렌더링, Game 뷰 키보드 포커스 및 실제 키 이벤트.
- 실제 플레이에서의 무게감·속도감, 고속 오버스티어 강도와 감속 후 회복감.
- 카메라 움직임의 편안함, 장시간 주행 안정성.
- Windows 독립 실행 파일 빌드.

타이어 설정은 오버스티어와 감속 후 접지 회복을 의도하지만 위 자동 검사만으로 주관적 주행감을 보장하지 않습니다. README의 코너링 비교 절차로 평가 후 튜닝하는 단계입니다.

## 오버스티어 변경 검증

열려 있는 Unity 프로젝트를 보존하기 위해 .physics-check 복사본에서 동일 Unity 버전으로 컴파일 및 물리 검사를 재실행했습니다. 저속과 고속의 최대 조향각 모두 32도를 확인했습니다. 속도 기반 조향 제한은 사용하지 않습니다. 이후 카운터스티어 변경에서 조건부 회전 감쇠 보조를 추가했습니다. 실제 드리프트 유지와 감속·카운터스티어 회복감은 직접 플레이 튜닝 대상입니다.


## 카운터스티어 완화 검증

Unity 6000.6.0f1 복사본 컴파일 및 물리 검사 통과 (countersteer-verify.log, 종료 코드 0). 고속 코너 후륜 최대 slip은 동일 검사에서 0.510 → 0.278로 감소했습니다. 전진 20m/s·횡속도 -8m/s·초기 회전 1.2rad/s 상태에서 반대키를 0.2초 적용한 비교는 보조 OFF 1.358rad/s, ON 0.764rad/s로 회전 감소를 확인했습니다. 이는 인위적으로 설정한 한 가지 미끄러짐의 비교이며 모든 코너에서의 회복을 보장하지 않습니다. 기존 가속·제동·후진·조향·핸드브레이크·복귀 검사도 통과했습니다. 실제 키보드 주행감은 플레이 확인이 필요합니다.


## 핸드브레이크 조향 변경

Unity 복사본에서 컴파일·기존 물리 검사 및 핸드브레이크 검사를 통과했습니다 (handbrake-verify.log, 종료 코드 0). 20m/s에서 W+Space+우측 조향을 0.5초 적용한 회전은 1.448rad/s, 이후 반대키 0.5초 적용 시 0.626rad/s로 감소했습니다. 전륜 구동 유지·후륜 구동 차단, Space 해제 즉시 브레이크 해제, 접지 강도 0.671에서 1.12까지 점진 복귀를 확인했습니다. 실제 화면에서의 드리프트 느낌과 지속 드리프트는 직접 플레이로 확인해야 합니다.


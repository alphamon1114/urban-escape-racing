# 주행 개선: 타이어 이동 방향을 반영한 조향

## 적용

Play 종료 → Unity 컴파일 완료 → 기존 World01 또는 DrivingTest 씬에서 다시 Play. 씬 재생성은 필요 없습니다.

기존의 높은 기본 접지력, Space 드리프트, 반대키 보조는 유지합니다. 일반 코너에서는 앞차축의 실제 이동 방향(차체 회전 포함)에 목표 미끄러짐 각도 12도를 더해 앞바퀴 조향 목표를 계산합니다. 저속에서는 기존 최대 32도까지, 정상 전진에서는 접지 상황에 맞는 각도로 부드럽게 전환합니다. Space 중에는 기존 조향을 사용하며 드리프트로 전환될수록 보정을 줄입니다. 입력이 없으면 중앙으로 돌아오고 반대키 전환 속도는 유지합니다.

추가 추진력·코너링 가속 보너스·속도 강제 복원은 없습니다. 엔진·기어·구동 배분은 이번 비교에서 변수를 줄이기 위해 변경하지 않았습니다. 이는 공개 자료에서 착안한 자체 구현으로, 포르자의 내부 수식이나 실제 차량 데이터를 복제한 것은 아닙니다.

## 개선 전후 측정

Unity 6000.6.0f1, 고정 스텝 0.02초, DrivingTest 평지. 매 시행 씬을 새로 로드하고 안착 후 진입 속도를 설정합니다. W와 우측 조향을 유지해 차체 누적 회전이 90도가 될 때까지 측정합니다. 좌표·속도·조향·회전 속도·전후륜 slip·드리프트 상태를 CSV에 기록합니다.

| 진입 속도 | 기존 탈출 속도 | 새 탈출 속도 | 기존 최소 속도 | 새 최소 속도 | 기존/새 회전 소요 시간 | 기존/새 근사 반경 |
|---|---|---|---|---|---|---|
| 60 km/h | 51.0 | 64.2 | 51.0 | 58.9 | 1.90 / 1.88초 | 18.8 / 20.4m |
| 90 km/h | 69.4 | 89.8 | 69.4 | 87.7 | 2.62 / 2.50초 | 36.5 / 39.3m |
| 120 km/h | 86.7 | 111.5 | 86.7 | 110.6 | 3.28 / 3.16초 | 59.1 / 63.1m |

반경은 이동 거리 / 누적 차체 회전량으로 계산한 근사치이며 일정 원을 정확히 추종한 검사는 아닙니다. 회전 반경은 약 7–9% 증가하므로 동일 경로에서 속도만 증가한 결과로 해석하면 안 됩니다. 다만 회전 소요 시간은 늘지 않았고 전륜 최대 slip은 60/90/120km/h에서 0.601/0.725/0.802 → 0.203/0.236/0.333으로 줄었습니다.

9도·12도·16도를 비교했습니다. 9도는 속도는 잘 유지하지만 반경과 시간이 크게 증가하여 제외했습니다. 12도는 속도 유지와 회전 반응의 절충값입니다. 더 날카로운 반응을 원하면 Inspector의 Corner Slip Angle을 16까지 조정할 수 있으나 코너 감속이 늘 수 있습니다.

## 비교와 검증 재실행

- Inspector의 Adaptive Steering을 끄면 기존 조향 방식과 비교할 수 있습니다. Play 중 변경은 종료 시 복원됩니다.
- 변경 전 컨트롤러 원문: `Docs/Baselines/SedanController-before-slip-steering.cs.txt`.
- 자동 비교: Unity batchmode에서 `-executeMethod HandlingBenchmark.Run`. 씬을 새로 열기 때문에 편집 중인 Editor에서 직접 실행하지 마세요. 별도 복사본에서 검사했습니다.
- 기록: `Docs/HandlingResults/*.csv`. `baseline`은 보정 OFF, `adaptive12`가 선정된 설정입니다. 9/16도 결과도 비교용으로 보관했습니다.
- 실제 Forza 텔레메트리는 확보하지 않았습니다. 원작 유사성, 도시의 실제 코너 완주, 사람의 키보드 조작감은 아직 직접 비교하지 않았습니다.

## 다음 평가

같은 도시 교차로에서 60–90km/h로 W+A/D 코너링을 하고 이전과 비교하세요. 조향 반경이 부담스러운지, Space 드리프트 및 반대키 반응이 유지되는지 우선 확인합니다. 이후 필요하면 엔진·변속기·차동장치를 별도 단계로 추가하고 동일 시험으로 비교합니다.
